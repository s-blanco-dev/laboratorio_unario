#include <stdio.h>
#include "../components/delay/include/delay.h"

#define ARRAY_SIZE 12

int exampleData;
char exampleArray[ARRAY_SIZE];

void app_main(void) {
    // probemos
    delay_ms(200);
    delay_s(200);
}
